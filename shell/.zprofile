# eval "$(/opt/homebrew/bin/brew shellenv)"
#
# export GRAALVM_HOME=$HOME/.sdkman/candidates/java/22.2.r17-grl
# export ANDROID_SDK_ROOT=$HOME/Library/Android/sdk
# export PATH="$HOME/.pub-cache/bin":"$ANDROID_SDK_ROOT/platform-tools":"$ANDROID_SDK_ROOT/emulator":"$ANDROID_SDK_ROOT/cmdline-tools/latest/bin":"/opt/homebrew/opt/libpq/bin":"$PATH"
# # export FVM_HOME=/Users/kiendang/.pub-cache/bin/fvm
# alias flutter='fvm flutter'
# alias dart='fvm dart'
#
# #nvm
# mkdir -p ~/.nvm
# export NVM_DIR="$HOME/.nvm"
# source $(brew --prefix nvm)/nvm.sh
# [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh" # This loads nvm
# [ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"  # This loads nvm bash_completion
#
# # Load rbenv automatically by appending
# # the following to ~/.zshrc:
# eval "$(rbenv init - zsh)"
#
# # libpq
# # For compilers to find libpq you may need to set:
# export LDFLAGS="-L/opt/homebrew/opt/libpq/lib"
# export CPPFLAGS="-I/opt/homebrew/opt/libpq/include"
#
# # For pkg-config to find libpq you may need to set:
# export PKG_CONFIG_PATH='/opt/homebrew/opt/libpq/lib/pkgconfig"
