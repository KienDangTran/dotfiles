# install.conf.yaml
- defaults:
    link:
      relink: true
      create: true
      force: true

- clean: ["~", "~/.config"]

- create:
    - ~/.vim

- link:
    ~/.wezterm.lua: config/wezterm/.wezterm.lua
    ~/.aliases: shell/.aliases
    ~/.zshrc: shell/.zshrc
    ~/.sleep: shell/.sleep
    ~/.wakeup: shell/.wakeup
    ~/.gitconfig: .gitconfig
    ~/.ideavimrc: shell/.ideavimrc
    ~/.markdownlint-cli2.yaml: shell/.markdownlint-cli2.yaml
    ~/.config:
      create: true
      relink: true
      glob: true
      path: config/**/**

# linux
- defaults:
    link:
      if: '[ "$(uname -s)" = "Linux" ]'

# mac
- defaults:
    link:
      if: '[ "$(uname -s)" = "Darwin" ]'
- link:
    ~/.gitlocal:
      path: git/mac
      force: true
    ~/Library/Application Support/lazygit/config.yml: config/lazygit/config.yml
    ~/Library/Application Support/jesseduffield/lazygit/config.yml: config/lazygit/config.yml

- shell:
    - description: apt-get install
      command: |
        if [ "$(uname -s)" = "Linux" ]; then
          sudo apt-get update
          sudo apt-get install -y zsh
        fi
      stdout: true
      stdin: true
    - [sudo chsh -s $(which zsh), Making zsh the default shell]
    - [git submodule update --init --recursive, Installing submodules]
